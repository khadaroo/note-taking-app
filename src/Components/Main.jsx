export default function Main({ children }) {
  return (
    <main className="mx-auto flex min-h-screen max-w-7xl items-center justify-center px-4 py-2.5 md:px-0 md:py-0">
      {children}
    </main>
  );
}
